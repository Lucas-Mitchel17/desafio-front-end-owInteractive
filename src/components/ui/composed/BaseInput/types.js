const AUTOCOMPLETE_ENUM = {
  OFF: 'off',
  ON: 'on',
};

const TYPE_ENUM = {
  DATE: 'date',
  EMAIL: 'email',
  HIDDEN: 'hidden',
  NUMBER: 'number',
  SEARCH: 'search',
  TEL: 'tel',
  TEXT: 'text',
};

const INPUT_TYPES = {
  AUTOCOMPLETE_ENUM,
  TYPE_ENUM,
};

export { AUTOCOMPLETE_ENUM, TYPE_ENUM, INPUT_TYPES };

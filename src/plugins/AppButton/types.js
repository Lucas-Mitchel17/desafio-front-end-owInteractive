const ROLES_ENUM = {
  BUTTON: 'button',
  SUBMIT: 'submit',
};

const TYPES_ENUM = {
  PROCEED: 'proceed',
  BACK: 'back',
  ADDCART: 'addCart',
};

const BUTTON_TYPES = {
  ROLES_ENUM,
  TYPES_ENUM,
};

export { ROLES_ENUM, TYPES_ENUM, BUTTON_TYPES };
